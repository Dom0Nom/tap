{
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @tap/web dev",
    "build": "pnpm --filter @tap/web build",
    "test": "pnpm --filter @tap/lib test",
    "test:all": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.5",
    "tsx": "^4.19"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3", "esbuild", "sharp"]
  }
}
